<p class="text-center"><%= @user.first_name %>, here you can add a profile pic.</p>
<h3 class="text-center">Your current photo :</h3>
<%if @user.avatar.attached?%>
  <%= image_tag @user.avatar, alt: 'avatar' %>
<%else%>
  <p class="text-center">=== You did not upload a pic yet ===</p>
<%end%>